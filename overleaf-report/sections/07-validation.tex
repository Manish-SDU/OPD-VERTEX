\section{Validation}
\label{sec:validation}

\subsection{Validation Strategy}
\label{subsec:validation-strategy}

The validation process follows a five-tier automated test pyramid plus a focused manual UI checklist. Unit tests exercise pure domain logic and the in-memory adapter layer. Integration tests drive FastAPI routes through Starlette's \texttt{TestClient} with a deterministic LLM mock, so HTTP behavior, validation, and persistence wiring are covered without external services. Smoke tests boot the full ASGI application and assert that critical-path endpoints, including login, dashboard, consultation list, and health checks, return successful responses.

Stress tests apply concurrent and sustained load to the in-memory repositories to surface ID collisions, race conditions, and memory growth. Benchmark tests measure per-operation latency. Two non-functional risks receive dedicated suites: LLM hallucination resistance and ASR artifact suppression. Both run hermetically against mocks in CI and can re-run live against a CUDA GPU when the local Ollama daemon and Whisper service are reachable.

\subsection{CI/CD Pipeline}
\label{subsec:cicd-pipeline}

The repository includes a GitHub Actions workflow in \path{.github/workflows/ci.yml}. It runs on pushes and pull requests targeting \path{main}, uses Python 3.12 with pip caching, and separates quality gates into independent jobs. The lint job installs the development dependencies and runs \path{ruff check app/} plus \path{ruff format --check app/}. Three test jobs then run the unit, integration, and smoke suites separately with \path{pytest}. The final Docker build job depends on all four quality gates and builds the application image tagged with the commit SHA.

The purpose of this pipeline is to protect a fast-moving team project from silent regressions. Med Flow combines FastAPI routes, application services, database adapters, AI-related mocks, Docker configuration, and UI templates, so a change in one area can easily break another. Continuous integration gives the team immediate feedback before a change is merged: formatting problems, failing tests, route regressions, and missing dependencies are caught in the same place for every contributor.

In this project, continuous delivery is intentionally limited to build verification rather than automatic production deployment. No production server is configured in the workflow. Instead, the delivery value is that every accepted change must prove it can become a Docker image. This provides a reproducible handoff for demonstrations, local deployment, and later production hardening without claiming an automated release process that the project does not yet implement.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  ciNode/.style={rounded corners=1.4mm, align=center, text width=2.55cm, minimum height=1.45cm, font=\small, inner sep=2.2mm},
  ciTrigger/.style={ciNode, draw=VertexBlue!75, fill=VertexBlue!7},
  ciGate/.style={ciNode, draw=VertexTeal!75, fill=VertexTeal!9},
  ciBuild/.style={ciNode, draw=VertexAmber!85, fill=VertexAmber!10},
  ciValue/.style={ciNode, draw=VertexGreen!75, fill=VertexGreen!9},
  ciArrow/.style={-{Latex[length=2.3mm]}, draw=VertexInk!65, line width=0.65pt}
]
\node[ciTrigger] (trigger) at (0,0) {\textbf{Code change}\\\scriptsize Push or pull request};
\node[ciGate] (gates) at (3.35,0) {\textbf{Quality gates}\\\scriptsize Ruff plus unit, integration, smoke tests};
\node[ciBuild] (build) at (6.70,0) {\textbf{Build check}\\\scriptsize Docker image can be produced};
\node[ciValue] (value) at (10.05,0) {\textbf{Project value}\\\scriptsize Safer merges and reproducible demos};

\draw[ciArrow] (trigger.east) -- (gates.west);
\draw[ciArrow] (gates.east) -- (build.west);
\draw[ciArrow] (build.east) -- (value.west);
\end{tikzpicture}
\caption{How the GitHub Actions pipeline turns each code change into quality feedback and a reproducible build candidate.}
\label{fig:cicd-pipeline}
\end{figure}

\subsection{Test Inventory and Outcome}
\label{subsec:test-inventory-outcome}

The automated source inventory currently contains 384 test functions across unit, integration, smoke, stress, and benchmark tiers. The generated validation artifact retained in the repository records an earlier 326-test execution with a 100.0\% pass rate; the final source inventory adds appointment and role-permission coverage for the booking workflow. Live-service tests gate themselves on the availability of Ollama or the Whisper microservice. On a developer workstation with both running, the live tier also passes.

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{@{}P{0.18\textwidth}P{0.10\textwidth}Y@{}}
\TableTopRule
\rowcolor{VertexTableBlue}
\VertexTableHead{Tier} & \VertexTableHead{$n$} & \VertexTableHead{Coverage focus} \\
\TableMidRule
\rowcolor{VertexTableStripe}
Unit & 275 & Domain logic, repositories, security, appointment scheduling, RBAC permissions, and hallucination guards. \\
Integration & 79 & HTTP routes, authentication flow, admin configuration, review API, and LLM contract. \\
\rowcolor{VertexTableStripe}
Smoke & 6 & Application boot, health endpoint, dashboard, and critical 2xx responses. \\
Stress & 14 & More than 1,000 creations, concurrent operations, ID-collision checks. \\
\rowcolor{VertexTableStripe}
Benchmarks & 10 & p50, p95, and p99 latency for 10 hot-path operations. \\
Manual UI & 10 & Login, PDF download, speaker layout, logout, and browser-level release checks. \\
\TableBottomRule
\end{tabularx}
\caption{Automated and manual validation tiers with test counts and coverage focus.}
\label{tab:validation-tiers}
\end{table}

The module-level inventory is retained in Appendix~\ref{app:test-inventory}. Raw benchmark numbers are listed in Appendix~\ref{app:benchmark-results}, the manual browser checklist in Appendix~\ref{app:manual-ui}, the AI safety matrix in Appendix~\ref{app:ai-safety-matrix}, and the hardware envelope in Appendix~\ref{app:hardware-envelope}.

\begin{figure}[htbp]
\centering
\optionalgraphic[width=0.78\textwidth]{assets/charts/test_distribution.png}
\caption{Automated validation distribution across unit, integration, smoke, stress, and benchmark tiers.}
\label{fig:test-distribution}
\end{figure}

\subsection{Requirements Coverage}
\label{subsec:requirements-coverage}

The validation evidence is tied back to the requirements from Section~\ref{sec:requirements} in Table~\ref{tab:requirements-coverage}. The table distinguishes requirements that were fully implemented from requirements that were deliberately left as deployment hardening or future work, so the report does not overclaim beyond the implemented system.

\begingroup
\scriptsize
\begin{longtable}{@{}P{0.13\textwidth}P{0.13\textwidth}P{0.34\textwidth}P{0.28\textwidth}@{}}
\caption{Requirements traceability and validation evidence.}\label{tab:requirements-coverage}\\
\TableTopRule
\rowcolor{VertexTableBlue}
\VertexTableHead{Requirement} & \VertexTableHead{Status} & \VertexTableHead{Achieved in report} & \VertexTableHead{Appendix / validation evidence} \\
\TableMidRule
\rowcolor{VertexTableStripe}
F-1 & Implemented & Doctor queue, booking context, and consultation creation are described in Section~\ref{subsec:actors-use-cases}, Section~\ref{subsec:system-overview}, Section~\ref{subsec:main-workflow-ai-pipeline}, Section~\ref{subsec:api-layer}, and Section~\ref{subsec:scope-decisions}. & Appendix~\ref{app:test-inventory} lists workflow and API tests; Appendix~\ref{app:manual-ui} includes TC003 and TC004. \\
F-2--F-3 & Implemented & Local audio capture and speech-to-text are covered in Section~\ref{subsec:main-workflow-ai-pipeline} and Section~\ref{subsec:transcription-module}. & Appendix~\ref{app:test-inventory} lists transcription and ASR tests; Appendix~\ref{app:hardware-envelope} documents the local runtime envelope. \\
\rowcolor{VertexTableStripe}
F-4 & Implemented & Structured note fields are defined in Section~\ref{subsubsec:functional-requirements} and designed in Section~\ref{subsec:main-workflow-ai-pipeline}; LLM orchestration and model selection are covered in Section~\ref{subsec:llm-orchestration} and Section~\ref{subsec:model-selection}. & Appendix~\ref{app:test-inventory} lists LLM and workflow tests; Appendix~\ref{app:ai-safety-matrix} maps generation risks to controls. \\
F-5--F-6 & Implemented & Prescription draft content, review alerts, editing, approval, and rejection are described in Section~\ref{subsec:main-workflow-ai-pipeline}, Section~\ref{subsec:ui-overview}, and Section~\ref{subsec:privacy-security-error-handling}. & Appendix~\ref{app:manual-ui} includes TC009; Appendix~\ref{app:test-inventory} includes versioning, suggestive-review, and workflow tests. \\
\rowcolor{VertexTableStripe}
F-7--F-8 & Implemented with deployment caveat & Approved-report PDF generation, email follow-up with the exported PDF attached, and prevention of unapproved email sends are covered in Section~\ref{subsec:pdf-generation}, Section~\ref{subsec:email-notification-service}, and Section~\ref{subsec:privacy-security-error-handling}. & Appendix~\ref{app:test-inventory} lists PDF/UI tests; Appendix~\ref{app:manual-ui} includes TC006. Production SMTP/TLS configuration remains a deployment concern. \\
F-9--F-10 & Implemented & Live transcript handling and past-consultation access are described in Section~\ref{subsec:transcription-module}, Section~\ref{subsec:main-workflow-ai-pipeline}, and Section~\ref{subsec:ui-overview}. & Appendix~\ref{app:test-inventory} lists transcription and workflow tests; Appendix~\ref{app:manual-ui} includes browser checks for consultation views. \\
\rowcolor{VertexTableStripe}
F-12 & Implemented internal flow and designed integration boundary & Internal appointment scheduling is described in Section~\ref{subsec:booking-scheduling-implementation}; replaceable booking and scheduling boundaries are described in Section~\ref{subsec:scope-decisions}. The implementation uses internal consultation context rather than a production public-sector booking provider. & Appendix~\ref{app:test-inventory} lists appointment and RBAC tests. No appendix claims production MitID/NemLog-in integration; that remains an adapter-level future enhancement. \\
F-11 & Future extension & Cross-doctor sharing is discussed as a possible extension in Section~\ref{subsec:scope-decisions} and Section~\ref{sec:discussion}. & No appendix claims completion for this item; it remains outside the primary workflow. \\
NF-1, NF-10 & Implemented & Local AI processing is justified in Section~\ref{subsec:ai-healthcare-security-privacy}, designed in Section~\ref{subsec:design-goals-principles}, and validated in Section~\ref{subsec:model-selection}. & Appendix~\ref{app:ai-safety-matrix} and Appendix~\ref{app:hardware-envelope} support the local model and hardware claims. \\
\rowcolor{VertexTableStripe}
NF-2 & Implemented & Authentication, role-based access, and patient-only access to own records are described in Section~\ref{subsec:authentication-authorization} and Section~\ref{subsec:privacy-security-error-handling}. & Appendix~\ref{app:test-inventory} lists auth and security tests; Appendix~\ref{app:manual-ui} includes TC001, TC002, and TC010. \\
NF-3 & Partially addressed & Password hashing, JWT cookies, and error boundaries are covered in Section~\ref{subsec:authentication-authorization} and Section~\ref{subsec:privacy-security-error-handling}; deployment hardening is discussed in Section~\ref{sec:discussion}. & Appendix~\ref{app:test-inventory} lists security tests. TLS termination, at-rest encryption, and service-to-service protection are future deployment work. \\
\rowcolor{VertexTableStripe}
NF-4 & Implemented & Replaceable components are described in Section~\ref{subsec:architectural-challenges}, Section~\ref{subsec:component-based-design}, and Section~\ref{sec:implementation}. & Appendix~\ref{app:test-inventory} lists component service, repository, and mock-service tests. \\
NF-5--NF-6 & Implemented & Persistence, versioning, and traceability are described in Section~\ref{subsec:data-design}, Section~\ref{subsec:database-integration}, Section~\ref{subsec:aop-cross-cutting}, Section~\ref{subsec:privacy-security-error-handling}, and Section~\ref{sec:implementation}. & Appendix~\ref{app:test-inventory} lists persistence alignment, repository, document-versioning, audit, and logging tests. \\
\rowcolor{VertexTableStripe}
NF-7--NF-8 & Implemented and measured & Usability is described in Section~\ref{subsec:ui-overview} and Section~\ref{subsec:manual-ui-validation}; performance is evaluated in Section~\ref{subsec:performance-benchmarking} and Section~\ref{subsec:hardware-deployment-envelope}. & Appendix~\ref{app:manual-ui}, Appendix~\ref{app:benchmark-results}, and Appendix~\ref{app:hardware-envelope} provide supporting evidence. \\
NF-9 & Future extension & Retention configuration and secure deletion are identified as governance extensions in Section~\ref{subsec:scope-decisions} and Section~\ref{sec:discussion}. & No appendix claims completion for this item; it remains future work. \\
\rowcolor{VertexTableStripe}
NF-11 & Implemented by workflow design & The human-in-the-loop boundary is stated in Section~\ref{subsec:delimitation}, designed in Section~\ref{subsec:main-workflow-ai-pipeline}, and discussed in Section~\ref{sec:discussion}. & Appendix~\ref{app:manual-ui} includes TC009; Appendix~\ref{app:ai-safety-matrix} documents safety controls that support doctor review. \\
\TableBottomRule
\end{longtable}
\endgroup

\subsection{LLM Hallucination Resistance}
\label{subsec:llm-hallucination-resistance}

Every LLM-backed component runs on open-source weights served locally by Ollama. The reference model is Qwen3:8b under the Apache 2.0 license. No proprietary cloud APIs are used, which keeps patient data on-premises and makes validation reproducible without a billing account.

Three failure classes are explicitly guarded:

\begin{enumerate}
  \item \textbf{Structural hallucinations:} malformed JSON, missing required keys, or a literal \texttt{None} rendered into a clinical field are caught by Pydantic validation in \texttt{OllamaClient.generate\_json}. The client retries once and then raises an error.
  \item \textbf{Content hallucinations:} invented allergies, diagnoses, or medications absent from the transcript are guarded by prompt-level grounding instructions and a post-generation contradiction check enforced in \texttt{test\_llm\_hallucination.py}.
  \item \textbf{Cross-consultation leakage:} text from a previous patient surfacing in the current note is prevented by stateless per-call prompts and verified by isolation tests that interleave generations from two synthetic patients.
\end{enumerate}

\texttt{test\_real\_llm.py} re-asserts the same invariants against the live Ollama daemon and falls back gracefully to the mock when the daemon is unreachable. The detailed AI and ASR safety matrix is kept in Appendix~\ref{app:ai-safety-matrix}.

\subsection{ASR Robustness}
\label{subsec:asr-robustness}

Faster-Whisper is tuned for clinical dictation with beam size 5, temperature 0, and the setting \path{condition_on_previous_text=False}. This reduces the decoder's tendency to echo earlier captions on silent or noisy audio. A WebRTC VAD at level 3 strips silence before the encoder, so the model does not receive empty buffers.

The Faster-Whisper hallucination test suite asserts four invariants: silence results in an empty transcript, known artifact strings such as ``Thanks for watching!'' and ``[Music]'' are dropped, word error rate stays below 0.15 on a synthetic clinical fixture, and two concurrent sessions never share partial-transcript state.

\subsection{Open-Source Model Selection}
\label{subsec:model-selection}

Six open-source models served by Ollama were ranked on four criteria: JSON-schema adherence, hallucination rate, end-to-end note latency at p95, and peak VRAM footprint. All candidates were quantized to \texttt{Q4\_K\_M} to fit the 8 GB VRAM envelope, evaluated on $n = 30$ synthetic consultations, and run at temperature 0.2.

\begin{table}[htbp]
\centering
\scriptsize
\begin{tabularx}{\textwidth}{@{}P{0.15\textwidth}P{0.09\textwidth}P{0.10\textwidth}P{0.11\textwidth}P{0.09\textwidth}P{0.10\textwidth}Y@{}}
\TableTopRule
\rowcolor{VertexTableBlue}
\VertexTableHead{Model} & \VertexTableHead{Params} & \VertexTableHead{VRAM} & \VertexTableHead{p95} & \VertexTableHead{JSON} & \VertexTableHead{Halluc.} & \VertexTableHead{Verdict} \\
\TableMidRule
\rowcolor{VertexTableStripe}
qwen3:8b & 8.2 B & 6.1 GB & 7.4 s & 100\% & 0/30 & \textbf{Selected}: strongest JSON validity and zero observed hallucinations inside the 8 GB target. \\
llama3.1:8b & 8.0 B & 5.8 GB & 6.9 s & 93\% & 2/30 & Runner-up with acceptable latency but weaker schema reliability. \\
\rowcolor{VertexTableStripe}
mistral:7b & 7.2 B & 5.0 GB & 5.7 s & 87\% & 3/30 & Weaker JSON adherence on structured clinical outputs. \\
phi3:medium & 14 B & 8.4 GB & 12.1 s & 96\% & 1/30 & Exceeded the 8 GB deployment budget on the reference GPU. \\
\rowcolor{VertexTableStripe}
gemma2:9b & 9.0 B & 6.6 GB & 8.8 s & 81\% & 4/30 & Prompt drift and less stable field extraction. \\
qwen3:4b & 4.0 B & 3.2 GB & 3.6 s & 71\% & 6/30 & Fast fallback, but too small for reliable clinical JSON generation. \\
\TableBottomRule
\end{tabularx}
\caption{Open-source model comparison for the Med Flow prompt set.}
\label{tab:model-selection}
\end{table}

\begin{figure}[htbp]
\centering
\optionalgraphic[width=0.82\textwidth]{assets/charts/model_comparison.png}
\caption{Model comparison visualization for the evaluated local Ollama candidates.}
\label{fig:model-comparison}
\end{figure}

Qwen3:8b was selected because it achieved 100\% JSON validity and zero hallucinations within the 8 GB VRAM budget. Phi3:medium was more accurate than smaller models but exceeded the budget, while the 4B model traded too much accuracy for speed. In the current implementation, the deployed model is read from \texttt{LLM\_MODEL\_NAME} and validated as \texttt{qwen3:8b} in \texttt{app/core/config.py}. Replacing it with another model would therefore require updating the configuration validator, prompt targets, and live LLM validation suite rather than only changing an environment variable.

\subsection{Hardware Constraints and Deployment Envelope}
\label{subsec:hardware-deployment-envelope}

The application was tested across the team's personal laptops to confirm that it runs on realistic consumer hardware rather than a single tuned reference machine. The tested devices included a Microsoft Surface Pro 11 with Snapdragon X Plus and 16 GB unified memory, a MINISFORUM UM890 Pro mini-PC with 32 GB DDR5 memory, an ASUS Vivobook 15 F1504VA with Intel Core 7 150U and 16 GB RAM, and an Apple MacBook Air 13-inch with 16 GB unified memory.

None of these devices has a discrete GPU, so the model stack is sized for a shared-memory profile. Whisper large-v3 in int8 uses approximately 3.0 GB and runs on CPU or NPU through \texttt{compute\_type=int8}. Qwen3:8b is served by Ollama at \texttt{Q4\_K\_M}, using approximately 6.1 GB peak memory, which fits within the 16 GB envelope of every device.

Latency varies by host, and the Surface and MacBook are roughly three to four times slower than a CUDA workstation. The application remains usable end-to-end. Reference latency numbers were collected separately on a CUDA workstation with an RTX 4060 Laptop GPU and 8 GB VRAM; the hardware profiles are detailed in Appendix~\ref{app:hardware-envelope}.

\subsection{Performance Benchmarking}
\label{subsec:performance-benchmarking}

The benchmark suite uses Python's \texttt{time.perf\_counter} to time 10 hot paths over 100 to 500 iterations each, recording p50, p95, and p99 latency. Each measurement has an explicit upper-bound assertion so a regression fails the build instead of silently degrading performance.

The numerical results are listed in Appendix~\ref{app:benchmark-results}, and Figure~\ref{fig:benchmark-latency} summarizes the latency distribution visually.

\begin{figure}[htbp]
\centering
\optionalgraphic[width=0.84\textwidth]{assets/charts/benchmark_latency.png}
\caption{Benchmark latency distribution for the measured hot-path operations.}
\label{fig:benchmark-latency}
\end{figure}

The results are favorable: every operation finishes in well under a millisecond at the 99th percentile. This indicates that the application layer is not the bottleneck. Production latency will be higher because real MySQL and MongoDB adapters add network and disk I/O, but the benchmark establishes that the domain logic imposes negligible overhead.

\subsection{Stress and Concurrency}
\label{subsec:stress-concurrency}

Stress testing verifies that the in-memory layer behaves correctly under load beyond what a single clinic shift would normally generate. Three scenarios run: 1,000 sequential patient creations to check for ID collisions and unbounded memory growth, 500 list-after-mutation invariant checks to confirm repository snapshots remain consistent, and concurrent consultation creation through \texttt{ThreadPoolExecutor} to expose race conditions in the create/list path.

All scenarios complete within their wall-clock budgets and assert post-conditions on count, uniqueness, and ordering.

\subsection{Manual UI Validation}
\label{subsec:manual-ui-validation}

Browser-level concerns that are awkward to automate with the current toolchain are covered by a 10-case manual checklist executed before each release. These concerns include cookie lifecycle, file downloads, CSS rendering of speaker turns, and layout responsiveness.

Each case is recorded with a unique identifier from TC001 to TC010, the scenario under test, reproduction steps, expected result, observed result, and status. At the time of writing, every case is passing. Future work proposes replacing this checklist with a Playwright end-to-end suite once the test infrastructure stabilizes.

\subsection{Threats to Validity}
\label{subsec:threats-to-validity}

Three threats temper the conclusions drawn from the validation results. First, benchmark figures use in-memory adapters, so production latency on MySQL and MongoDB will be higher. These numbers should be read as a lower bound that proves the domain code is not the bottleneck, not as production service-level objectives.

Second, live LLM tests are opportunistic. When Ollama is unavailable, they fall back to a deterministic mock and are reported as passing. A green CI run alone therefore does not prove that the live model still satisfies the hallucination guards; the live tier must be re-run on a developer workstation before each release.

Third, the WER fixture is synthetic. The published Faster-Whisper large-v3 envelope is used as ground truth for accuracy claims, and a real annotated clinical corpus is the most important validation improvement.

\subsection{Future Validation Work}
\label{subsec:future-validation-work}

Future validation work includes WER measurement on a real annotated clinical corpus, mutation testing with \texttt{mutmut} on the domain core, property-based testing with \texttt{hypothesis} on status transitions, Playwright end-to-end tests replacing the manual UI checklist, and JMeter-based load testing for the deployed HTTP API.